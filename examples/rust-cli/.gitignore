# Proof #4 (rust-cli) build output — never commit. Root-anchored so a bare
# 'target' never matches a hypothetical source directory named target.
/target
# intermediate render frames (regenerated by demo/capture + tools/proof-render-demo.py)
demo/.frames/
