# Proof #3 (expr-parser) build artifacts — never commit these.
# NOTE: bare `expr` would also match the include/expr/ source dir, so these are
# root-anchored to the proof's binary outputs.
/expr
/expr.exe
/expr_tests
/expr_tests.exe
*.o
*.exe
# demo render staging frames
demo/.frames