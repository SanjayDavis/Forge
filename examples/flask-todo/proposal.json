{
  "proposal_id": "prop_flask_001",
  "reason": "Decompose Flask task-manager app: factory -> schema -> models -> routes -> views -> tests",
  "confidence": 0.9,
  "events": [
    {"op": "task_created", "id": "app-factory", "title": "Flask app factory and config",
     "description": "create_app() factory, config dict, instance dir, register /health route",
     "acceptance": ["create_app() returns a Flask instance", "/health returns 200 with JSON body"],
     "files": ["app/__init__.py", "run.py"], "priority": "high", "notes": []},
    {"op": "task_created", "id": "db-schema", "title": "SQLite schema and init",
     "description": "schema.sql with tasks table (id, title, done, created_at); init_db() creates tables",
     "acceptance": ["init_db() creates the tasks table", "schema is idempotent"],
     "files": ["db.py", "schema.sql"], "priority": "high", "notes": []},
    {"op": "task_created", "id": "models", "title": "Task data-access layer",
     "description": "add_task/get_all/mark_done/delete over sqlite3 with row factory",
     "acceptance": ["add_task persists and returns an id", "get_all returns ordered rows", "mark_done and delete work"],
     "files": ["models.py"], "priority": "high", "notes": []},
    {"op": "task_created", "id": "routes", "title": "HTTP routes",
     "description": "GET / (list), POST /add (create), POST /done/<id>, POST /delete/<id>",
     "acceptance": ["POST /add redirects to /", "POST /done/<id> marks done", "unknown id returns 404"],
     "files": ["app/routes.py"], "priority": "high", "notes": []},
    {"op": "task_created", "id": "templates", "title": "Templates",
     "description": "base.html + index.html rendering task list with add/complete/delete forms",
     "acceptance": ["index renders all tasks", "empty list shows empty state"],
     "files": ["app/templates/base.html", "app/templates/index.html"], "priority": "medium", "notes": []},
    {"op": "task_created", "id": "static-css", "title": "Minimal stylesheet",
     "description": "static/style.css — clean default styling, no framework",
     "acceptance": ["stylesheet is linked in base.html", "done tasks render with strikethrough"],
     "files": ["app/static/style.css"], "priority": "low", "notes": []},
    {"op": "task_created", "id": "tests", "title": "Test suite",
     "description": "unittest over Flask test client: health, list, add, done, delete, 404",
     "acceptance": ["all tests pass", "covers every route"],
     "files": ["tests/test_app.py"], "priority": "high", "notes": []},
    {"op": "task_created", "id": "readme", "title": "README",
     "description": "setup, run, and test instructions",
     "acceptance": ["documents pip install, run, test commands"],
     "files": ["README.md"], "priority": "low", "notes": []},
    {"op": "dependency_added", "task": "db-schema", "depends_on": "app-factory"},
    {"op": "dependency_added", "task": "models", "depends_on": "db-schema"},
    {"op": "dependency_added", "task": "routes", "depends_on": "models"},
    {"op": "dependency_added", "task": "templates", "depends_on": "routes"},
    {"op": "dependency_added", "task": "static-css", "depends_on": "templates"},
    {"op": "dependency_added", "task": "tests", "depends_on": "routes"},
    {"op": "dependency_added", "task": "readme", "depends_on": "tests"}
  ]
}
