__pycache__/
*.egg-info/
*.pyc
build/
dist/
.venv/
events.lock
events.log

# proofs ship their raw event log as evidence
!examples/**/events.log
demo/.frames/
dist/
.venv-test/

# local planning scratch — never commit (contains machine-local paths)
.hermes/

# transient proof driver (Proof #4 lifecycle)
drive_proof4.py
